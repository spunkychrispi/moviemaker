package {
	
	/**
	 * Returns the filename without the extension
	 */
	public function filename(fullFilename:String):* {
	    var extensionIndex:Number = fullFilename.lastIndexOf( '.' );
        var extensionless:String = fullFilename.substr( 0, extensionIndex ); 
        return extensionless;
	}
}